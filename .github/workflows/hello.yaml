name: Hello                       # ワークフロー名
on: push                          # どのタイミングでワークフローが実施されるか(イベント)
jobs:                             # 実施されるジョブの定義
  hello:                          # ジョブID
    run-on: ubuntu-latest         # ランナー(実行環境)
    steps:                        # ステップの定義
      - run: echo "Hello, world"  # shaellコマンドの実行
      - uese: action/checkout@v4  # actionの呼び出し
  
